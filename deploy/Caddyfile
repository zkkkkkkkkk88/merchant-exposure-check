:80 {
    basic_auth argon2id {
        {$AUTH_USERNAME} {$AUTH_PASSWORD_HASH}
    }

    handle_path /api/* {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy web:3000
    }
}
